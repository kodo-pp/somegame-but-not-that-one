#!/usr/bin/env python
from setuptools import setup


setup(
    name = 'somegame',
    author = 'kodopp',
    version = '0.0.0',
    packages = ['somegame'],
    install_requires = ['pygame', 'loguru'],
)
